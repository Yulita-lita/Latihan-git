// latihan for 
for (var angka = 1; angka < 10; angka++) {
    console.log ("Iterasi ke-" + angka);

}
var jumlah = 0
for (deret = 5; deret > 0; deret--) {
    jumlah += deret
    console.log ("jumlah saat ini "+ jumlah);
}
console.log ("jumlah terakhir :" + jumlah);

// latihan while 

var flag = 1
do {
    console.log ("Iterasi ke-" + flag);
    flag++;
}   while (flag < 10)




//Tugas Latihan For 

    for ( var flag = 2; flag <=10; flag += 2){
        console.log ("Iterasi for ke: " + flag);
    }

// Tugas Latihan While 
    var flag = 2;
    while (flag <=10) {
        console.log ("Iterasi while ke:" + flag);
        flag+=2;
    }

    var flag = 10;
    while (flag > 1) {
        console.log ("Iterasi while ke:" + flag);
        flag-=2;
    }

    
